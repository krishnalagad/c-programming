#include "Functionalities.hpp"

/*
    Client code
        g++ Functionalities.cpp Main.cpp -g -o app && time ./app && rm app
*/
int main() {
    Container data;
    createObjects(data);

    return 0;
}